name: ${name}
main: ${mainClass}
version: ${version}
author: ${author}
api-version: ${apiVersion}
prefix: ${prefix}

commands:
  enchantextractor:
    description: EnchantExtractor configuration command
    permission: enchantextractor.reload

  extract:
    description: Extract a enchantment from items you are holding.
    aliases:
      - e
    permission: enchantextractor.extract